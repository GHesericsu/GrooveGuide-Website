import styled from 'styled-components';

const Container = styled.div`
  width: 100%;
`

const Wrapper = styled.div`
  max-width: 400px;
  margin: auto;
`

const NextButton = styled.button`

`

const PreviousButton = styled.button`

`

const ChangeWeekButtons = () => {
  return {
    
  }

}